{
  "name": "sufal-personal-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run deploy:production",
    "deploy:production": "npm run build && wrangler deploy --env ''",
    "deploy:test": "npm run build && wrangler deploy --env test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.4"
  },
  "allowScripts": {
    "workerd@1.20260921.1": true,
    "esbuild@0.28.2": true
  },
  "devDependencies": {
    "wrangler": "^4.138.0"
  }
}
